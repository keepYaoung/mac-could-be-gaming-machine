# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode / Swift
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
.swiftpm/
.build/
Packages/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Wine prefixes / runtime artifacts (never commit — huge, machine-specific)
*.app/Contents/Resources/wineprefix/
wineprefix/
prefixes/
*.wineprefix
drive_c/

# Downloaded binaries / runtimes (fetched, not vendored)
runtimes/bin/
*.dmg
*.pkg
*.tar.xz
*.tar.gz

# D3DMetal — proprietary, NC-only, redistribution forbidden. NEVER commit/bundle.
# Loaded as a user-provided external component on the user's own machine.
D3DMetal/
**/D3DMetal*
*.d3dmetal

# Logs
*.log
logs/
